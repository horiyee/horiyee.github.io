# lab

Hono + Vite SSG foundation for [lab.horiy.dev](https://lab.horiy.dev).

Experiments, content pipeline, and deploy come later.

| Script | Description |
| --- | --- |
| `pnpm dev` | Local development |
| `pnpm build` | Generate static site into `dist/` |
| `pnpm preview` | Preview the production build |
| `pnpm typecheck` | TypeScript check |

```text
src/
  index.tsx       # Hono app / routes
  components/     # Layout
  pages/          # Page views
```
